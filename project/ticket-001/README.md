# Ticket 001: Taskand: versioned DSL envelopes and controlled complementary evolution

- **ID**: ticket-001
- **Owner**: unresolved:human
- **Status**: IN_PROGRESS
- **Workflow state**: EDIT
- **Created**: 2026-09-13

## Goal and scope

Session authorization: the user's 2026-09-13 request authorizes local source
updates in wellmanifest/taskand and standard compatibility analysis. Agent:
taskand (Codex). The follow-up authorizes preventing unpublished change growth
and updating the standard; prior protected-publication authority remains valid.
Continue this ticket and checkout, without allocating another worktree.

Add closed, independently versioned request/log contracts and a documented
bounded evolution lifecycle. Preserve v1 and runtime ownership outside wellmanifest.
Risks: consumers must opt in; JSON Schema alone grants no execution authority.
The bounded continuation adds a closed delivery observation and a pure reference
evaluator with regression fixtures, within the existing 15-file budget. It does
not implement Git effects or install unpublished policy in consumers.
The current continuation qualifies bounded incident routing and durable
no-progress budgets in the same reference evaluator, schema and tests. No
new material files, worktree, runtime daemon or consumer activation are added.
The two optional root seed aliases project.sh/project.bat are omitted; their
byte-identical primary copies and a restore-verified private snapshot remain.
The managed project/governance-check.* entry points are unchanged. This keeps
the full material delivery at the original 15-file limit rather than raising it.

## Acceptance criteria

- [ ] AC-01: Version, policy and normative bundle agree; contract fixtures pass.
- [ ] AC-02: Documentation separates observed glm53 capabilities from requirements;
  exact-path governance check reports the remaining deployment limitations.

## Tracking boundary

Continuation result 2026-09-13: TKD-013–014 and TKD-EVO-005–010, a closed delivery
observation and reference evaluator are implemented locally; 46/46 tests,
legacy conformance 9/9 and bundle verification pass. Full pending-scope
governance has zero findings at the unchanged 15-material-file limit. The
separate worktree guard still rejects duplicate pending adoption paths in
primary and ticket-001 (GOV-WORKTREE-OVERLAP-001). No commit, push, merge,
consumer adoption or Taskand deployment is claimed. Canonical detail is in
docs/information/evolution.md. Private receipt group:
receipt:sha256:42704b687bd195783e444dcde4d37dcff55cbdcf93144884b506d58231c7e717
(private audit operational-recovery-20260913.n8Qm9z).
Test-result SHA-256: 2ee8b6dd056a2a090c166e20b8b6f3ace0597984d8cf0a04e156c1231af5de3d.
Resume with current ownership/fencing and lossless adoption reconciliation,
not a new feature, worktree, larger budget or weakened guard.

Handoff 2026-09-13: Local source implementation and validation are complete; no commit or publication was requested. This is an unpublished candidate, not a trusted terminal receipt. Reacquire a matching lease and revalidate before any further edit or authorized publication.
Evidence: private audit `receipt:taskand/audits/complementary-20260913-0920/audit.json`
(`sha256:a9bc8245b78b565765e690e7d8726a0cfc32cdb671ecf359a85284b4a9c3569d`).
`decisions.md` records a local check only, never trusted review approval.

This directory contains the minimal reviewed intent. Optional participant prose
and raw command logs are not required delivery output.
